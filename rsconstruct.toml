# rsconstruct build configuration for this repository.

[analyzer.python]

[processor.ruff]
src_dirs = ["src", "scripts"]

[processor.pylint]
src_dirs = ["src", "scripts"]

[processor.mypy]
src_dirs = ["src", "scripts"]

[processor.taplo]
src_dirs = ["."]

# The Makefile compiled src/**/*.java together with
# `javac -Xlint:all <sources> -d bin`. scripts/javac_build.py runs that exact
# command; the explicit processor triggers it on any .java change.
[processor.explicit.java]
command = "scripts/javac_build.py"
input_globs = ["src/**/*.java"]
output_dirs = ["bin"]

# lint the GitHub Actions workflow files with actionlint
[processor.actionlint]
src_dirs = [".github/workflows"]

# ─── markdown ────────────────────────────────────────────────────────────────

[processor.rumdl]
src_files = ["README.md"]

# ─── java ────────────────────────────────────────────────────────────────────

[processor.checkstyle]
src_dirs = ["src"]
args = ["-c", "checkstyle.xml"]

# ─── html ────────────────────────────────────────────────────────────────────

[processor.htmlhint]
src_dirs = ["doc"]
